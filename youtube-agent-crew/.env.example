# === LLM Providers ===
OPENAI_API_KEY=sk-xxx
GOOGLE_AI_API_KEY=xxx
ANTHROPIC_API_KEY=xxx

# === Supabase (Brain System + Pipeline Checkpoints) ===
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_ANON_KEY=xxx

# === YouTube Data API ===
YOUTUBE_API_KEY=xxx

# === YouTube Upload (OAuth2 — required for auto-upload) ===
# Create credentials at https://console.cloud.google.com/apis/credentials
# Enable "YouTube Data API v3" in your GCP project
# Run OAuth flow once to get refresh token (see docs/youtube-oauth.md)
YOUTUBE_CLIENT_ID=xxx.apps.googleusercontent.com
YOUTUBE_CLIENT_SECRET=xxx
YOUTUBE_REFRESH_TOKEN=xxx
# YOUTUBE_ACCESS_TOKEN is auto-managed at runtime; set manually only if needed
# YOUTUBE_ACCESS_TOKEN=xxx

# === TTS — VoxCPM-1.5-VN (replaced Fish Speech) ===
VOXCPM_API_URL=http://localhost:8100

# === ElevenLabs (alternative TTS) ===
ELEVENLABS_API_KEY=xxx
ELEVENLABS_VOICE_ID=xxx

# === LongSang Admin API ===
ADMIN_API_URL=http://localhost:3001

# === Langfuse Observability (optional — set to enable LLM tracing) ===
# LANGFUSE_SECRET_KEY=sk-lf-xxx
# LANGFUSE_PUBLIC_KEY=pk-lf-xxx
# LANGFUSE_HOST=https://cloud.langfuse.com

# === Pipeline Controls ===
# PIPELINE_MAX_COST=0.50
# CHECKPOINT_DIR=./output/.checkpoints

# === Telegram Notifications (pipeline alerts to your personal chat) ===
# Create a bot via @BotFather, send /start to it, then get your chat ID from @userinfobot
TELEGRAM_BOT_TOKEN=xxx
TELEGRAM_CHAT_ID=xxx
# Admin dashboard URL for links in notifications (defaults to http://localhost:5173/admin)
# ADMIN_DASHBOARD_URL=http://localhost:5173/admin

# === Auto-Seeding (post-pipeline social distribution) ===
# Telegram channel announcement (separate from the notification bot above)
TELEGRAM_SEED_BOT_TOKEN=xxx
TELEGRAM_SEED_CHANNEL_ID=@your_channel

# Twitter/X API v2 (OAuth 2.0 Bearer Token)
TWITTER_BEARER_TOKEN=xxx

# Facebook Page posting
FB_PAGE_TOKEN=xxx
FB_PAGE_ID=xxx

# Reddit
REDDIT_ACCESS_TOKEN=xxx
REDDIT_SUBREDDIT=your_subreddit

# Forlike.pro engagement seeding
FORLIKE_TOKEN=xxx
FORLIKE_BASE_URL=http://my.forlike.pro

# === Output ===
OUTPUT_DIR=./output
