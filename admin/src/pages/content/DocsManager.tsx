/**
 * 📚 Documentation Manager Page
 * Admin page for managing documentation across all projects
 */

import { DocsManagerDashboard } from '@/components/docs/DocsManagerDashboard';

export default function DocsManager() {
  return <DocsManagerDashboard />;
}
